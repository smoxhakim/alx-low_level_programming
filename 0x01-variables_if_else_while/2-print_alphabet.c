/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char star = 'a';
	char end = 'z';

	while(char cont = star; cont <= end; cont++)
	{
		putchar(cont);
	}

	putchar('\n');

	return (0);

}
