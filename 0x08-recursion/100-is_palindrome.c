#include "main.h"

/**
 * is_palindrome - check if palindrome
 * @s: input
 * Return: 1 or 0
 * owned by: SmoxHakim
 */

int is_palindrome(char *s)
{
	if (*s == " ")
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
