#include "main,h"

/**
 * mul - prnit the multiplies of tow intiger
 * @a: the first input
 * @b: the second input
 * Return: the result
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

