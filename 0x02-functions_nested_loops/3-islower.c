#include "main.h"
#include <ctype.h>

/**
 * _islower - checks for lowercase character
 * return 0
 *
 */

int _islower(int c)
{
	char ch = 'a';

	if (islower(ch))
		_putchar('1');
	}
	else
	{
		_prunchar('0');
	}
	return (0);
}

